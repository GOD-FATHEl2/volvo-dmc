# DISABLED: Azure Static Web Apps CI/CD
# This workflow is disabled because the Azure Static Web App resource is no longer available
# Use the Azure App Service deployment instead for the full Flask application

name: Azure Static Web Apps CI/CD (DISABLED)

on:
  workflow_dispatch: # Only run manually
  # push:
  #   branches:
  #     - main
  # pull_request:
  #   types: [opened, synchronize, reopened, closed]
  #   branches:
  #     - main

jobs:
  build_and_deploy_job:
    if: false # DISABLED - workflow is disabled
    runs-on: ubuntu-latest
    name: Build and Deploy Job (DISABLED)
    permissions:
       id-token: write
       contents: read
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled. Use Azure App Service deployment instead."

  close_pull_request_job:
    if: false # DISABLED - workflow is disabled
    runs-on: ubuntu-latest
    name: Close Pull Request Job (DISABLED)
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled. Use Azure App Service deployment instead."
