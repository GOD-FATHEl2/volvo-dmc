# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: volvo-dmc-generator
metadata:
  template: volvo-dmc-generator@0.0.1-beta
  
services:
  app:
    project: ./
    language: python
    host: containerapp
    docker:
      context: ./
      dockerfile: ./Dockerfile
